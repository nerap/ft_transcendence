<div id="main-chatrooms">

  <div>
    <%= link_to new_chatroom_path do %>
      Create a chatroom
    <% end %>
  </div>

  <% if @chatrooms.present? %>
  <% else %>
    <div>
      There are no chatrooms
    </div>
  <% end %>

  <div>
    <ul class="tabs">
      <li class="active"><a href="#public" data-turbolinks="false">Public chatrooms</a></li>
      <li><a href="#private" data-turbolinks="false">Private chatrooms</a></li>
    </ul>
    <div class="tabs-content">
      <div class="tab-content active" id="public">
        <table class="tab-content-list">
          <tr>
            <th>CHATROOM NAME</th>
            <th>CREATED BY</th>
          </tr>
          <% @chatrooms.each do |chatroom| %>
            <% if chatroom.chatroom_type == "public" %>
              <tr class="tab-content-options">
                <td><%= link_to chatroom_path(chatroom), class: "tab-content-option-link" do %><div class="tab-content-option"><%= chatroom.name %></div><% end %></td>
                <td><%= link_to User.find(chatroom.owner), class: "tab-content-option-link" do %><div class="tab-content-option"><%= User.find(chatroom.owner).username %></div><% end %></td>
              </tr>
            <% end %>
          <% end %>
        </table>
      </div>
      <div class="tab-content" id="private">
        <table class="tab-content-list">
          <tr class="tab-content-headers">
            <th class="tab-content-header">CHATROOM NAME</th>
            <th class="tab-content-header">CREATED BY</th>
          </tr>
          <% @chatrooms.each do |chatroom| %>
            <% if chatroom.chatroom_type == "private" %>
              <tr class="tab-content-options">
                <td class="chatroom-link"><div id="tab-content-option-link" do %><div class="tab-content-option"><%= chatroom.name %></div></div></td>
                <td><%= link_to User.find(chatroom.owner), class: "tab-content-option-link" do %><div class="tab-content-option"><%= User.find(chatroom.owner).username %></div><% end %></td>
                <!-- Modal -->
                <%= render "login", chatroom: chatroom %>
                <!-- End Modal -->
              </tr>
            <% end %>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>

<%= javascript_pack_tag 'Chatrooms.js' %>