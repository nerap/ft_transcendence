<div id="users-index">
    <table>
        <tr>
            <th colspan="4">Current Guild war History</th>
        </tr>
        <% Transcendence.guild_wars.each(function(bob) { %>
        <% if (bob.toJSON().done == "true") { %>
        <% if ((bob.toJSON().guild_one_id == guild.id) || (bob.toJSON().guild_two_id == guild.id)) { %>
        <% }}}) %>
    </table>
</div>
